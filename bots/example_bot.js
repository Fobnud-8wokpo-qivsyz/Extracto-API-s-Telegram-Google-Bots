/**
 * Ejemplo básico de bot Telegram
 * Reemplaza TOKEN y empieza a construir.
 * Estilo Grok: simple, claro y listo para crecer.
 */

// const TelegramBot = require('node-telegram-bot-api');
// const token = process.env.TELEGRAM_BOT_TOKEN;
// const bot = new TelegramBot(token, { polling: true });

// bot.onText(/\/start/, (msg) => {
//   bot.sendMessage(msg.chat.id, '¡Hola desde Extracto-API-s! Estilo Grok activado 🚀');
// });

console.log('Bot de ejemplo listo. Descomenta e instala node-telegram-bot-api para usar.');
